import React from 'react';

const Ambassadors = () => (
  <section className="p-10 bg-gray-100">
    <h2 className="text-2xl font-semibold mb-4">Student Ambassadors</h2>
    <p>Chat or book calls with real students from your dream university.</p>
    {/* Add logic to list ambassadors */}
  </section>
);

export default Ambassadors;
